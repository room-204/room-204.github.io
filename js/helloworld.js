function popupPicture(index){
    const popup = document.getElementById('popup');
    switch(index) {
        case 0: {
            popup.firstChild.
        }
    }
    popup.style.opacity = 1;
    alert('damaya');
}